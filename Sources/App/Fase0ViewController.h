#import <UIKit/UIKit.h>

// Tela única da Fase 0: confirma que o app roda no aparelho e executa o
// teste de dlopen da dylib dummy embarcada em Frameworks/ (decide D3).
@interface Fase0ViewController : UIViewController
@end
